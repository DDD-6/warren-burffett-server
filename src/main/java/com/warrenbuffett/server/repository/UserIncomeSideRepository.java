package com.warrenbuffett.server.repository;

import com.warrenbuffett.server.domain.UserIncomeSide;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserIncomeSideRepository extends JpaRepository<UserIncomeSide,Long> {
}
